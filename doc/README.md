# 三模型 block 执行入口

计划：`three_model_typical_block_closure_20260917_zh.yaml`。
逐项状态：`block_checklist.yaml`，**唯一执行状态来源**。

计划完整迁入本目录，内容/门禁不变；旧 `plans/` 路径为指向本文件的兼容符号链接，不再维护副本。计划内的文件路径仍可经兼容链接访问，其“待执行”是编制时快照，不用于调度。实际状态只读 checklist 的 `to do`、`ongoing`、`done`。

44 个原任务编号保留。独立可验收的先行工作拆成子项，写清前置、负责人、完成范围与证据；子项部分完成只使父项为 ongoing，不得标为整项 done。历史 SiLU 组件结果只完成 P01.1；P01.2 的完整 Host+iDMA A/B 尚未完成。设计默认开关不变。

本地 agent 开始前先同步 main 并读取 checklist：不重新实现 done 的内容，不抢占他人 ongoing 的子项；执行依赖已满足的 to do 项。需要对已完成组件做新的**集成**回归时，用对应集成任务记录，不重新将组件开发置为待办。缺权重、工具或长仿真条件写入说明，状态仍为 to do；已经有真实部分产物的父项为 ongoing，不新增 blocked/PASS 等状态。

本轮正在先行实现的子项为 C00.1（源字节核对）、C01.1（配置入口校验）、C03.1（派生几何合同）、P00.1（归档计数与带宽上界检查器）。这些不依赖本地 agent 输出；官方权重/forward核对、完整iDMA长仿真和STA仍为对应未完成任务。代码与测试通过后更新子项证据和状态，不预先标 done。
