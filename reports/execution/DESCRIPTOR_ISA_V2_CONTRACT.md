# Descriptor/ISA v2 contract gate

Result: PASS.

The 128-bit command layout is unchanged. Descriptor schema v2 freezes NULL
unused roots, opcode-specific root roles, pre-issue snapshot rules, tensor
stream tag/ID derivation, completion status values and records `matrix_aux`,
`dma_policy` and `event_list4`. Python typed records reject unknown types,
nonzero common subtype/flags, reserved bits, zero subarray masks, zero DMA
limits and invalid barrier events.

All example segments were migrated and their committed command reports were
regenerated. Descriptor and command vectors are generated by deterministic
scripts with independent `--check` mode.

Validation bound to CPU 8-25:

- full Python suite: 57 PASS;
- descriptor vector generate/check: PASS;
- three segment report generate/check: PASS;
- Python compileall: PASS;
- schema non-overlap and 128-bit bounds: PASS.

This closes only the contract checkpoint. Production multi-op sequencer RTL
remains the next L2 gate.
