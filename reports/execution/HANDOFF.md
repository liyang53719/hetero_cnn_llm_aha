# L11 execution handoff

- Canonical stages are L0-L11 from `reports/ARCHITECTURE_AND_EXECUTION_PLAN.md`.
- Current phase: L2 wrapper-only macro integration.
- Every compile, simulation, synthesis, and Docker command uses `taskset -c 8-25`; Docker additionally uses `--cpuset-cpus=8-23`.
- L0 PASS: 27 Python tests, C++ reference, RTL contract simulation, and five 1.0 ns CLN22UL contract DC tops passed. The integrated contract top has WNS 9.75132e-05 ns and zero unmapped cells.
- Docker proxy is now functioning for `hello-world`, and the pinned StanfordAHA image was pulled at `sha256:a8784f2cfe96609a7e4403c29f6a82bd00c882c8564ef747541f78be75fa2b2b`.
- AHA 4x16 image source equals the clean host source at `3b171e813bc5e399b22921c8df20fd4e889f1569`. Gaussian generation, map, PnR and its VCS test all pass; VCS reports a 14.7275 us run and an integer bit-accurate comparison.
- Strict AHA Verilator closure PASS: source-locked 5.028, GCC10/C++20 coroutine support, 4x16 Gaussian generation/map/PnR/bitstream, Vtop, and integer bit-accurate output comparison all pass. Evidence is `work/results/l1_aha_verilator_5028_cxx10_makeflags/` and `aha_verilator_result_audit.json`.
- `scripts/write_used_upstream_lock.py` PASSed. It locks the actual Gemmini and AHA Gaussian/PnR dependency closure and intentionally excludes unrelated Chipyard/ Voyager LFS subtrees.
- Fresh Gemmini evidence: Spike mvin/mvout PASS, Spike OS-matmul PASS, Verilator mvin/mvout PASS (669 us), and Verilator OS-matmul PASS (10 ms, 577.7 s wall).
- Spike ResNet50 also passes in explicit `os` mode: 3,053,813 cycles and final `PASS`. The previous `tohost=1337` was a default WS-mode run, not an architectural functional mismatch.
- L1 upstream reproduction PASS. Canonical full WS uses native `LOADMEM=1` and finishes at 10 ms within the one allowed 20M cap; see `L1_UPSTREAM_CLOSEOUT.md`. The default HTIF loader WFI limitation remains documented but does not alter binary/RTL semantics.
- L2 macro boundaries are frozen, not closed: generated Gemmini is locked at 157 ports (SHA256 `8ae6fd2e...130ce66f0`) with retained RocketTile command router/PTW/TileLink context. `gemmini_rocc_command_adapter` remains a clean-room-only test adapter and must not be connected to the macro.
- The pinned AHA 4x16 `Garnet` top was regenerated as SHA256 `4980ce62...0bf0ab354`; its 33 ports, nested 69-port `Interconnect`, and 25-file generated simulator closure passed host Verilator 5.050 complete named-port lint. The wrapper must use the Garnet AXI/proc-packet boundary. Native 17-bit data lanes and independent 1-bit EOS lanes remain distinct inside Garnet; do not map `last` into bit 16.
- The official Gaussian map/PnR trace export is PASS: 93 generated app files include `gaussian.bs`, metadata, placement and routes. Its concrete 16-bit lanes are input x={0,2}, output x={1,3}; output payload/valid ports are `io2glb_17_X01_Y00` and `io2glb_17_X03_Y00`. Use the metadata, not a fixed generic lane assumption.
- AHA macro wrapper RTL has begun with `aha_garnet_axi_config_loader`: it implements only the official single-write AW→W→B AXI flow and passes randomized-ready directed testing for configuration/stream-start writes. Next is a separate 512→64 proc-packet writer using official Global Buffer address metadata.
- `aha_garnet_proc_packet_writer` now exists and passes: it emits eight low-word-first 64-bit proc-packet writes for each 512-bit beat, preserving resolved base address and byte enables. The remaining AHA L2 work is the control/run FSM plus metadata-driven packet bases and full Gaussian trace equivalence.
- `aha_garnet_microsequencer` now composes packet-write, AXI-write and official interrupt-wait micro-ops, with a directed ordered test PASS. The remaining AHA L2 gap is to lower actual Gaussian metadata/bitstream into this transcript and run it against the full Garnet top.
- Gaussian bitstream lowering now has a partial PASS: 519 official `{addr,data}` entries become 65 lossless 512-bit packet beats (final beat: seven words). The unverified `bs_cfg/kernel_cfg` AXI register table remains explicitly open; do not run a claimed Gaussian wrapper result until it is extracted from the official runtime.
- The `bs_cfg/kernel_cfg` table is now official and machine-derived: compiled pinned AHA parser/map/gen produces five bs_cfg plus 39 kernel_cfg AXI writes, PCFG start `0x01c=1`, and stream start `0x018=0x00030003`. The next task is a full Garnet-top harness that consumes this transcript; numerical equivalence is still open.
- Gaussian input payload transcript is now exact under the upstream default rule: 25,344 raw bytes split by interleaved 16-bit elements into two 12,672-byte blocks at packet bases `0x00000` and `0x20000` (198 512-bit packets each). Full Garnet-top execution/output comparison remains open.
- The parser/map-derived pre-PCFG interrupt enables are fixed in the transcript: `0x02c=7`, `0x028=1`, `0x024=3`, `0x020=3`; this is required before waiting on the Garnet PCFG interrupt.
- A full generated-Garnet transcript harness now elaborates 644 modules and builds a 33 MiB Verilator executable. Its real `Garnet` PCFG runtime probe now PASSes: it accepts interrupt enables, all 519 bitstream entries, BS/kernel configuration writes, the PCFG start pulse, and the official interrupt in 2253 cycles. The fix is an explicit 0->1->0 `reset_in` sequence: declaration-time high initialization did not execute the generated AXI controller's asynchronous AWREADY reset branch in Verilator. The full input/output numerical comparison remains open.
- `gemmini_rocc_lowering.py` now has a typed resolved INT8 output-stationary single-tile view. It emits official CUSTOM_3 configuration/load/preload/compute/store fields and has 6 passing exact-encoding tests. It intentionally rejects >16 and WS cases until the retained-RocketTile descriptor allocator/harness exists; no C-route/macro numerical equivalence is claimed yet.
- `lc_shell` was not found; it is only a future L10 SRAM-library dependency.
- Resource guard: a global OOM was recorded while MATLAB was the invoking process and ChatGPT/VS Code were killed. All subsequent heavy project work uses `scripts/run_memory_capped.sh`: 16 GiB admission floor, `MemoryHigh=10G`, `MemoryMax=14G`, `taskset -c 8-25`, and `-j2` for Garnet/Verilator builds.
- AHA output readback is intentionally still FAIL, not PASS: exact G2F and F2G ISR masks both reach `0x3`, but the first 64-bit read at official output address `0x10000` is zero instead of golden `0x710070006d007000`. The harness now includes official mapper-derived output addresses, CGRA stall/unstall, ISR-specific polling, and reset/drain sequencing. Diagnose the first zero without relaxing the golden.
- Do not close any stage using the existing clean-room-only evidence.
