{
  "phase": "L5_Q128_PREFILL",
  "command": "Parameterize the proven q128 QKV batch0 TB with +BATCH=N while keeping one unchanged RTL binary. Generate operation-order goldens independently for batches1-7 from the same shared input hash 39917c9e6f887030091cb93e944b263f4966322f487d567a6c28881b615335e0 and exact target weights. Run batches1-7 sequentially with one resource admission check per batch; each must report 98304 Matrix steps and exact norm/Q/K/V hashes. Aggregate all eight receipts to 786432 measured QKV steps and freeze concatenated q128 Q/K/V hashes before starting RoPE.",
  "fixed_order": [
    "q128 shape and causal-operation manifest",
    "q128 synthesizable trace controller",
    "q128 RMSNorm/QKV payload batches",
    "q128 split-half RoPE and post-RoPE GQA",
    "q128 streamed causal M/L/O and O/L",
    "q128 OProj/residual/norm2",
    "q128 gate/up/SiLU/product/down/final",
    "q128 receipt aggregate"
  ],
  "success_condition": "The q128 manifest and controller use exact causal counts; every numerical phase consumes and emits frozen hashes; no score matrix is materialized; measured Matrix/SFU/controller cycles remain separately classified; all 128 final-token vectors pass the operation-order model under CPU 8-25, max j4 and 10 GiB cap.",
  "failure_policy": "Do not derive q128 measurements by scaling target-token results, omit causal prefixes, replace 16-row physical batching with a software shortcut, materialize scores, relax first mismatches, or start q384 before q128 numerical and controller receipts both pass."
}
