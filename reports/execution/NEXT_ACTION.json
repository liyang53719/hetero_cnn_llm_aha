{
  "phase": "L5_Q384_PREFILL",
  "command": "Extend the existing runtime QKV vector generator and TB path to workload1024 without adding or recompiling workload-specific RTL. Generate64 restartable16-token batches, run all64 on one binary, require98304 Matrix steps and401504 cycles per batch, aggregate6291456 steps and25696256 cycles, then rerun q128 and q384 compatibility on the same binary.",
  "fixed_order": [
    "q128 shape and causal-operation manifest",
    "q128 synthesizable trace controller",
    "q128 RMSNorm/QKV payload batches",
    "q128 split-half RoPE and post-RoPE GQA",
    "q128 streamed causal M/L/O and O/L",
    "q128 OProj/residual/norm2",
    "q128 gate/up/SiLU/product/down/final",
    "q128 receipt aggregate"
  ],
  "success_condition": "One unchanged QKV RTL binary passes all64 q1024 batches and q128/q384 compatibility, emits exact Q/K/V hashes and measured cycles, and stays within CPU8-23 and30GiB.",
  "failure_policy": "Do not compile q1024-specific RTL, scale q384 outputs, materialize attention scores, relax first mismatches, or start q1024 RoPE before all QKV batches and compatibility receipts pass."
}
