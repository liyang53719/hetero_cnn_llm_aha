{
  "phase": "L5_Q384_PREFILL",
  "command": "Wrap the unified runtime prefill count controller with measured per-operation latency selected by runtime sequence length. In one compiled binary run q128 and q384, require61101824 and202769664 active cycles,11698176 and35094528 Matrix steps,24 commands each, zero score-matrix commands, and reject sequence256. Then close q384 and begin q1024 runtime extension without forking RTL.",
  "fixed_order": [
    "q128 shape and causal-operation manifest",
    "q128 synthesizable trace controller",
    "q128 RMSNorm/QKV payload batches",
    "q128 split-half RoPE and post-RoPE GQA",
    "q128 streamed causal M/L/O and O/L",
    "q128 OProj/residual/norm2",
    "q128 gate/up/SiLU/product/down/final",
    "q128 receipt aggregate"
  ],
  "success_condition": "One unchanged runtime-configured controller reproduces every measured q128/q384 operation latency and aggregate cycle/count total with no score matrix; q384 closeout cites only passed payload and measured RTL evidence.",
  "failure_policy": "Do not scale q128 totals to invent q384 latency, compile sequence-specific RTL, hide per-operation mismatches, or begin q1024 before q384 payload and trace receipts both pass."
}
