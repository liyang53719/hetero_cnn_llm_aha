{
  "phase": "L4_PRODUCTION_AHA_4X4_LAKE_DEPTHWISE",
  "command": "Audit the pinned AHA/Garnet/Lake generator for the exact production geometry garnet_width=4, garnet_height=4 and lake_memory_tiles=16 while preserving the 512 KiB CGRA SRAM budget. Generate and hash the RTL/bitstream dependencies without modifying canonical upstream. Then map a deterministic INT8 3x3 depthwise tile, run official map/PnR/bitstream and Verilator numerical flow, wrap it with the existing config/proc-packet/sideband endpoint, and record elementwise output hash, RTL cycles, semantic/physical bytes and canonical conflicts. If this exact generator combination is unsupported, record BLOCKED_DEPENDENCY; do not substitute the L2 4x16 Gaussian baseline.",
  "fixed_order": [
    "INT8 GEMM",
    "1x1 Conv",
    "3x3 Conv",
    "bias requant ReLU",
    "pool residual",
    "production 4x4 AHA Lake depthwise",
    "depthwise pointwise",
    "ResNet50 and MobileNetV2 representative blocks"
  ],
  "success_condition": "Pinned dependency and weight hashes are recorded; deterministic INT8 GEMM cases are elementwise bit-exact from operator golden through production descriptor RTL and canonical L3 top; reported RTL cycles/DMA bytes/utilization/conflicts come from counters rather than analysis estimates; all builds/tests remain CPU 8-25, max j4 and under the 10 GiB cap.",
  "failure_policy": "Do not skip ahead to model blocks, use analytical numbers in RTL-measured fields, substitute the 4x16 Gaussian baseline for the required production 4x4 AHA/Lake, relax integer equality, change the frozen descriptor/quantization contract, or mark L4 PASS before every ordered subgate is complete."
}
