{
  "phase": "L4_INT8_GEMM_CANONICAL_L3_TRACE",
  "command": "Replay the exact 17x18x19 OS descriptor and DMA request shapes through hetero_l3_production_top: descriptor/control reads use logical read client 0, six weight beats use client 1, activation plus bias beats use client 2, and five output writes use logical write client 0. Run the clients concurrently, complete the Matrix command only after all responses/writes, and record canonical-top cycles, physical bytes, bank conflicts/stalls and command completion ordering. Merge those measured fields with reports/execution/l4_int8_gemm_payload_result.json; do not rerun or reinterpret payload cycles as L3 cycles.",
  "fixed_order": [
    "INT8 GEMM",
    "1x1 Conv",
    "3x3 Conv",
    "bias requant ReLU",
    "pool residual",
    "production 4x4 AHA Lake depthwise",
    "depthwise pointwise",
    "ResNet50 and MobileNetV2 representative blocks"
  ],
  "success_condition": "Pinned dependency and weight hashes are recorded; deterministic INT8 GEMM cases are elementwise bit-exact from operator golden through production descriptor RTL and canonical L3 top; reported RTL cycles/DMA bytes/utilization/conflicts come from counters rather than analysis estimates; all builds/tests remain CPU 8-25, max j4 and under the 10 GiB cap.",
  "failure_policy": "Do not skip ahead to model blocks, use analytical numbers in RTL-measured fields, substitute the 4x16 Gaussian baseline for the required production 4x4 AHA/Lake, relax integer equality, change the frozen descriptor/quantization contract, or mark L4 PASS before every ordered subgate is complete."
}
