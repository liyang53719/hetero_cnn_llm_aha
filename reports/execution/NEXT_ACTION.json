{
  "phase": "L5_Q384_PREFILL",
  "command": "BLOCKED_DECISION: after user approval, implement hierarchical online-softmax block128 for sequence lengths above384 in the existing runtime M/L/O RTL. First run a Python operation-order candidate and require max error<=0.002; then add the summary-merge micro-op, verify43008 q1024 merges with no score storage, and rerun q128/q384 legacy hashes plus q1024 in one binary.",
  "fixed_order": [
    "q128 shape and causal-operation manifest",
    "q128 synthesizable trace controller",
    "q128 RMSNorm/QKV payload batches",
    "q128 split-half RoPE and post-RoPE GQA",
    "q128 streamed causal M/L/O and O/L",
    "q128 OProj/residual/norm2",
    "q128 gate/up/SiLU/product/down/final",
    "q128 receipt aggregate"
  ],
  "success_condition": "The approved same-binary hierarchical mode passes the frozen0.002 threshold and all operation-order/hash/counter checks while q128/q384 legacy receipts remain unchanged.",
  "failure_policy": "Do not relax0.002, claim the1024-segment PWL candidate passed, compile q1024-specific RTL, materialize scores, or continue to OProj before this decision and numerical gate close."
}
