// SPDX-License-Identifier: Apache-2.0
module fp32_mlo_summary_merge_stream_rawpipe#(parameter integer LANES=4)(input logic clk_i,rst_ni,input logic header_valid_i,output logic header_ready_o,input logic[31:0]ma_i,la_i,mb_i,lb_i,input logic beat_valid_i,output logic beat_ready_o,input logic[LANES*32-1:0]oa_i,ob_i,input logic beat_last_i,output logic header_valid_o,input logic header_ready_i,output logic[31:0]m_o,l_o,output logic beat_valid_o,input logic beat_ready_i,output logic[LANES*32-1:0]o_o,output logic beat_last_o);
 typedef enum logic[1:0]{IDLE,COEFF,HEADER,BEATS}state_t;state_t state_q;logic civ,cir,cov,cor;logic[31:0]m,l,alpha,beta;logic biv,bir,bov,bol;
 assign civ=state_q==IDLE&&header_valid_i;assign header_ready_o=state_q==IDLE&&cir;assign cor=state_q==COEFF;fp32_mlo_merge_coeff_rawpipe coeff(.clk_i,.rst_ni,.in_valid_i(civ),.in_ready_o(cir),.ma_i,.la_i,.mb_i,.lb_i,.out_valid_o(cov),.out_ready_i(cor),.m_o(m),.l_o(l),.alpha_o(alpha),.beta_o(beta),.flags_o());assign header_valid_o=state_q==HEADER;assign m_o=m;assign l_o=l;assign biv=state_q==BEATS&&beat_valid_i;assign beat_ready_o=state_q==BEATS&&bir;fp32_mlo_merge_beat_rawpipe#(.LANES(LANES))beat(.clk_i,.rst_ni,.in_valid_i(biv),.in_ready_o(bir),.alpha_i(alpha),.beta_i(beta),.oa_i,.ob_i,.last_i(beat_last_i),.out_valid_o(bov),.out_ready_i(beat_ready_i),.o_o,.last_o(bol));assign beat_valid_o=bov;assign beat_last_o=bol;
 always_ff@(posedge clk_i or negedge rst_ni)if(!rst_ni)state_q<=IDLE;else case(state_q)IDLE:if(civ&&cir)state_q<=COEFF;COEFF:if(cov&&cor)state_q<=HEADER;HEADER:if(header_ready_i)state_q<=BEATS;BEATS:if(bov&&beat_ready_i&&bol)state_q<=IDLE;default:state_q<=IDLE;endcase
endmodule
