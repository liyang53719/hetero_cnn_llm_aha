#!/usr/bin/env python3
import argparse,json
from pathlib import Path
def req(x,m):
 if not x:raise SystemExit('Q1024_CONTRACT_FAIL '+m)
def main():
 a=argparse.ArgumentParser();a.add_argument('--contract',type=Path,required=True);a.add_argument('--result',type=Path,required=True);x=a.parse_args();c=json.loads(x.contract.read_text());s=c['shape'];p=c['physical_array'];m=c['matrix_steps'];w=c['stream_work'];rb=(s['sequence']+p['rows']-1)//p['rows'];kv=s['kv_heads']*s['head_dim'];e={'q':rb*s['hidden']*(s['hidden']//32),'k':rb*s['hidden']*(kv//32),'v':rb*s['hidden']*(kv//32),'o':rb*s['hidden']*(s['hidden']//32),'gate':rb*s['hidden']*(s['intermediate']//32),'up':rb*s['hidden']*(s['intermediate']//32),'down':rb*s['intermediate']*(s['hidden']//32)};req(rb==p['row_batches']==64,'batches');req(all(m[k]==v for k,v in e.items())and m['total']==sum(e.values())==93585408,'matrix');cs=1024*1025//2;up=cs*12;req(cs==524800 and up==w['causal_head_token_updates']==6297600,'causal');req(w['rope_pairs_total']==917504 and w['reciprocals']==12288 and w['normalization_chunks']==98304,'attention');req(w['silu_scalars']==9175040 and w['product_chunks']==573440,'mlp');req(c['storage_policy']['score_matrix_materialized']is False and c['cycle_policy']['rtl_measured_cycles']is None,'policy');r={'stage':'L5','subgate':'q1024 prefill operation contract','status':'PASS','evidence_class':c['evidence_class'],'row_batches':rb,'matrix_steps':m['total'],'causal_head_token_updates':up,'score_matrix_materialized':False,'rtl_measured_cycles':None};x.result.parent.mkdir(parents=True,exist_ok=True);x.result.write_text(json.dumps(r,indent=2)+'\n');print('L5_Q1024_CONTRACT_PASS row_batches=64 matrix_steps=93585408 causal_updates=6297600 measured_cycles=null')
if __name__=='__main__':main()
